# خطة تطوير الميزات الجديدة

## الميزتان المطلوبتان

### 1. نظام المصروفات (Expenses Module)
**الهدف:** تسجيل المصروفات (الإيجار، الفواتير، الأجور) وتأثيرها المباشر على الصناديق والأداء المالي

**المتطلبات:**
- دالة جديدة: `recordExpense()`
- تأثير مباشر على رصيد الصندوق
- تأثير على قائمة الأداء المالي
- عدم التأثير على فواتير المبيعات/المشتريات
- معالجة محاسبية صحيحة

**التأثير المحاسبي:**
```
Expense Entry:
├─ Debit: Expense Account (نفقات - قائمة الأداء)
└─ Credit: Cash Box (صندوق - ينخفض الرصيد)
```

**المعادلات:**
```
boxBalance = boxBalance - expenseAmount
expensesTotal += expenseAmount
netProfit = salesRevenue - expensesTotal - costOfGoods
```

### 2. تحسين كشوفات الحساب (Enhanced Account Statements)
**الهدف:** عرض كشف حساب دقيق مع الترتيب الزمني والأرصدة التراكمية

**المتطلبات:**
- دالة: `getPartnerStatement(partnerId, dateRange)`
- دالة: `getCashBoxStatement(boxId, dateRange)`
- ترتيب زمني صحيح (تصاعدي)
- رصيد افتتاحي دقيق
- أرصدة تراكمية صحيحة
- معالجة العمليات بشكل صحيح

**الهيكل:**
```typescript
{
  opening_balance: number,       // الرصيد الافتتاحي
  transactions: [
    {
      date: string,
      description: string,
      debit: number,
      credit: number,
      balance: number            // الرصيد المتراكم
    }
  ],
  closing_balance: number        // الرصيد الختامي
}
```

---

## خطوات التنفيذ

### المرحلة 1: إضافة أنواع البيانات
- إضافة `Expense` interface
- إضافة `ExpenseCategory` type
- إضافة `StatementEntry` interface
- تحديث `TransactionSourceType`

### المرحلة 2: إضافة الدوال المالية
- `recordExpense()` في `financialEngine.ts`
- `getPartnerStatement()` في `financialUtils.ts`
- `getCashBoxStatement()` في `financialUtils.ts`
- دوال مساعدة للحسابات

### المرحلة 3: تكامل قاعدة البيانات
- إضافة `expenses` collection في Firestore
- إضافة معالجة في `db.ts`
- إضافة المزامقة في `localDb.ts`
- إضافة معالجة الحذف الآمن

### المرحلة 4: الاختبارات والتحقق
- اختبار تسجيل المصروفات
- اختبار كشوفات الحساب
- اختبار الأرصدة التراكمية
- اختبار الترتيب الزمني

---

## حماية البيانات

✅ ما سيتم:
- إضافة نماذج جديدة فقط
- توسيع الدوال الموجودة
- إضافة معالجات جديدة

✅ ما لن يتم:
- تعديل البيانات القديمة
- تصفير أي حسابات
- تغيير واجهات المستخدم
- تعديل ملفات CSS/Tailwind

---

## معايير النجاح

1. **المصروفات:**
   - يتم تسجيل المصروفات بنجاح
   - الرصيد ينخفض بشكل صحيح
   - الأداء المالي يتأثر بشكل صحيح
   - لا تؤثر على فواتير المبيعات

2. **كشوفات الحساب:**
   - الترتيب الزمني صحيح
   - الأرصدة التراكمية دقيقة
   - الرصيد الافتتاحي صحيح
   - الرصيد الختامي يطابق قاعدة البيانات

---

## الجدول الزمني

- **المرحلة 1:** 15 دقيقة
- **المرحلة 2:** 45 دقيقة
- **المرحلة 3:** 30 دقيقة
- **المرحلة 4:** 30 دقيقة

**الإجمالي:** ساعتان تقريباً
