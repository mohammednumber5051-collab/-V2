# خطة التصحيح الآمنة للأولويات 1 و 2

**التاريخ:** 24 يوليو 2026
**الحالة:** ✅ جاهزة للتنفيذ
**مستوى الخطر:** منخفض جداً (لا تعديل على البيانات الحالية)

---

## 🔍 التحليل الأولي - ما تحتاج معرفته

### الوضع الحالي:
- ✅ البيانات الحقيقية **محفوظة تماماً** في Firestore/LocalStorage
- ✅ لا توجد عمليات Initialization تمسح البيانات
- ✅ الدوال حالياً **حسابات فقط** لا تعدل البيانات المباشرة

### ما سيحدث بعد الإصلاح:
- ✅ نفس البيانات الموجودة ستبقى موجودة
- ✅ **فقط** الحسابات الجديدة **القادمة** ستستخدم الصيغ الصحيحة
- ✅ البيانات التاريخية ستبقى كما هي (ليس لدينا خيار آخر بدون مراجعة يدوية)
- ⚠️ **ملاحظة هامة:** التقارير قد تكون مختلفة عن السابق!

---

## 📋 الملفات التي سيتم تعديلها

### 1. `src/lib/financialUtils.ts`
**السبب:** توحيد حساب الأرصدة والخصومات
**المتأثرون:** `calculateUnifiedPartnerBalances()`

### 2. `src/services/financialEngine.ts`
**السبب:** إصلاح Debit/Credit المعاكس
**المتأثرون:** `getInvoiceImpact()` و `getQuickEntryImpact()`

---

## 🔧 التغييرات المحددة

### المشكلة 1️⃣: الخصومات لا تُطرح من الأرصدة

**الموقع:** `src/lib/financialUtils.ts:line ~80`

```typescript
// ❌ الحالي (خطأ):
const netAmount = (inv.total || 0) - (inv.discount || 0);
partnerBalances[inv.partnerId].total += (netAmount * sign);
partnerBalances[inv.partnerId].paid += ((inv.paid || 0) * sign);
// remaining = total - paid = (1000 - 100) - 500 = 400 ✓
```

**ملاحظة:** الكود الحالي **صحيح في الواقع**! لكن المشكلة في مكان آخر.

### المشكلة 2️⃣: معادلات Debit/Credit معاكسة

**الموقع:** `src/services/financialEngine.ts:line ~47-96`

```typescript
// ❌ الحالي (خطأ في Sales Invoice):
{
    type: 'قبض',
    debit: netAmount,      // ❌ يجب credit!
    credit: 0,             // ❌ يجب debit: 0!
}

// ❌ الحالي (خطأ جزئي في Purchase Invoice):
{
    type: 'صرف',
    debit: 0,              // ✓ صحيح
    credit: netAmount,     // ✓ صحيح
}
```

**المعادلة المحاسبية:**
```
فاتورة مبيعات:
✅ Debit: Accounts Receivable (الذمم المدينة) = 1000
✅ Credit: Sales Revenue (الإيراد) = 1000

❌ الكود الحالي معاكس!

فاتورة مشتريات:
✅ Debit: Purchases (المشتريات) = 1000
✅ Credit: Accounts Payable (الذمم الدائنة) = 1000

❌ الكود الحالي معاكس!
```

---

## 🛡️ خطة الحماية

### ✅ ما لن نعدله:
1. قاعدة البيانات الحالية (Firestore)
2. LocalStorage الحالي
3. واجهات المستخدم (UI)
4. أي ملفات تصميم (CSS/Tailwind)
5. البيانات التاريخية الموجودة

### ✅ ما الذي سيتغير:
1. الصيغ المستخدمة في الحسابات الجديدة
2. كيفية عرض Debit/Credit في القيود الجديدة
3. معاملة الفواتير الجديدة (وليس القديمة)

### ⚠️ التحذيرات:
1. التقارير **قد** تختلف عن السابق
2. الأرصدة السابقة **لن تتغير تلقائياً**
3. قد تحتاج مراجعة محاسبية للبيانات التاريخية

---

## 📊 جدول التغييرات المحددة

| الملف | الدالة | السطر | التغيير | التأثير |
|------|--------|-------|---------|---------|
| financialEngine.ts | getInvoiceImpact | 47 | Sales: debit ↔ credit | ✅ معادلة صحيحة |
| financialEngine.ts | getInvoiceImpact | 70 | Purchase: بقاء كما هو | ✓ صحيح فعلاً |
| financialEngine.ts | getQuickEntryImpact | 130 | توحيد Debit/Credit | ✅ معادلة صحيحة |
| financialUtils.ts | calculateUnified... | 80-95 | إضافة تعليقات توضيح | 📝 وثائق فقط |

---

## 🔄 العملية الآمنة

### قبل البدء:
```
1. ✅ قراءة هذة الخطة
2. ✅ فهم التأثيرات المحتملة
3. ✅ موافقة صاحب المشروع
4. ✅ نسخة احتياطية (اختياري - البيانات في Firestore آمنة)
```

### أثناء التطبيق:
```
1. ✅ تعديل ملف واحد في المرة
2. ✅ اختبار الدالة المعدلة فوراً
3. ✅ التحقق من عدم حدوث أخطاء
4. ✅ فحص البيانات الموجودة (عدم المس بها)
```

### بعد التطبيق:
```
1. ✅ اختبار معاملات جديدة
2. ✅ مقارنة النتائج مع المتوقع
3. ✅ مراجعة التقارير
4. ✅ موافقة نهائية
```

---

## ⏱️ الجدول الزمني

- **الملف 1 (financialUtils.ts):** 10 دقائق
- **الملف 2 (financialEngine.ts):** 20 دقيقة
- **الاختبار الأولي:** 15 دقيقة
- **الإجمالي:** 45 دقيقة

---

## ✅ قائمة التحقق قبل البدء

- [ ] قرأت هذه الخطة كاملة
- [ ] فهمت المشاكل الدقيقة
- [ ] فهمت الحلول المقترحة
- [ ] فهمت التأثيرات المحتملة
- [ ] جاهز للبدء

---

## 🎯 النتيجة النهائية المتوقعة

### ✅ بعد الإصلاح:
1. فواتير المبيعات الجديدة: Debit صحيح ✓
2. فواتير المشتريات الجديدة: Credit صحيح ✓
3. الأرصدة: محسوبة بشكل صحيح ✓
4. التقارير: دقيقة وموثوقة ✓
5. البيانات القديمة: محفوظة كما هي ✓

---

## 📌 ملاحظات مهمة

1. **البيانات آمنة:** لن نحذف أو نمسح أي شيء
2. **التغييرات تدريجية:** الفواتير الجديدة فقط
3. **لا تأثير على الواجهات:** لا نعدل UI
4. **قابل للعكس:** يمكننا الرجوع إذا لزم الأمر

---

**جاهزون للبدء؟ اضغط "نعم" وسأبدأ بالتعديلات الآمنة!**
